#!/bin/sh
#
# if you require the use of a proxy server, configure it here
# the following are examples of things you might want to do to configure the
# virtual machines to use a proxy server. customize accordingly...
#

#echo 'export https_proxy="http://proxy.example.edu:3128/"' >> /etc/environment
#echo 'export http_proxy="http://proxy.example.edu:3128/"' >> /etc/environment

#echo 'Acquire::http::Proxy "http://proxy.example.edu:3128/";' > /etc/apt/apt.conf.d/01proxy
